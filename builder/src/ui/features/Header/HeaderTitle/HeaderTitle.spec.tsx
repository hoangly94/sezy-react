import { render } from '@testing-library/react'

describe('HeaderTitle', () => {
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  test('renders the HeaderTitle with enough elements', () => {})
})
