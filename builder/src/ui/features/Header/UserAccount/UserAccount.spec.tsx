import { render } from '@testing-library/react'

describe('UserAccount', () => {
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  test('renders the UserAccount with enough elements', () => {})
})
